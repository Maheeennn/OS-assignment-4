# OS-assignment-4
This assignment teaches how to automate C/C++ program compilation using a Makefile and benchmark sequential and parallel implementations. It involves running Sequential, Pthreads, OpenMP, and MPI versions of matrix multiplication and a sorting algorithm across multiple input sizes, with results collected automatically.
